{
  "config": {
    "step": {
      "user": {
        "title": "SpatialHA",
        "description": "Configure SpatialHA (optional MQTT settings for SpatialBLE devices).",
        "data": {
          "mqtt_host": "MQTT Broker Hostname",
          "mqtt_port": "MQTT Broker Port",
          "mqtt_username": "MQTT Username",
          "mqtt_password": "MQTT Password"
        }
      }
    },
    "abort": {
      "single_instance_allowed": "Only a single instance of SpatialHA is allowed."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "SpatialHA Options",
        "description": "Configure MQTT broker for SpatialBLE devices.",
        "data": {
          "mqtt_host": "MQTT Broker Hostname",
          "mqtt_port": "MQTT Broker Port",
          "mqtt_username": "MQTT Username",
          "mqtt_password": "MQTT Password"
        }
      }
    }
  }
}
